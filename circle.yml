general:
  artifacts:
    - "coverage"

machine:
  environment:
    NPM_CONFIG_PROGRESS: false
    MOCHA_FILE: $CIRCLE_TEST_REPORTS/mocha/tests.[hash].xml

notify:
  webhooks:
    - url: http://bot.bubobox.com/publish/circleci/master
